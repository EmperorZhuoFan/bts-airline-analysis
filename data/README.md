# Data folder

Place the flight dataset here as `AIRLINE_BTS.csv`.

It is not included in the repository because it is ~126 MB, which exceeds
GitHub's 100 MB per-file limit. Download it from BTS TranStats
(Reporting Carrier On-Time Performance table) — see the main README.
